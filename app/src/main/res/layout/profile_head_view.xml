<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:id="@+id/layout_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_gravity="bottom"
                android:gravity="bottom">

    <ImageView
        android:id="@+id/iv_user_head"
        android:layout_width="@dimen/distance_space55"
        android:layout_height="@dimen/distance_space55"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="55dp"
        android:contentDescription="@string/app_name"/>

    <TextView
        android:id="@+id/tv_user_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/iv_user_head"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/distance_space10"
        android:text="赵小虎"
        android:textColor="#ffffff"
        android:textSize="16sp"/>

    <LinearLayout
        android:id="@+id/ll_action_button"
        android:layout_width="match_parent"
        android:layout_height="@dimen/distance_space50"
        android:layout_alignParentBottom="true"
        android:background="#66000000"
        android:divider="@mipmap/ic_img_line_v"
        android:dividerPadding="16dp"
        android:showDividers="middle">

        <TextView
            android:id="@+id/tv_register"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_weight="1"
            android:gravity="center"
            android:text="注册"
            android:textColor="#ffffff"
            android:textSize="15sp"/>

        <TextView
            android:id="@+id/tv_login"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_weight="1"
            android:gravity="center"
            android:text="登录"
            android:textColor="#ffffff"
            android:textSize="15sp"/>
    </LinearLayout>

    <ImageView
        android:id="@+id/iv_user_setting"
        android:layout_width="@dimen/distance_space25"
        android:layout_height="@dimen/distance_space25"
        android:layout_alignParentRight="true"
        android:layout_marginRight="@dimen/distance_space15"
        android:layout_marginTop="@dimen/distance_space15"
        android:src="@mipmap/icon_setting"/>
</RelativeLayout>